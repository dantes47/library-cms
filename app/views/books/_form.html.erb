<%= simple_form_for @book do |fld| %>
  <%= fld.input :title, label: 'Book Title' %>
  <%= fld.input :description %>
  <%= fld.input :author %>
  <%= fld.input :coauthor %>

  <%= fld.button :submit %>
<% end %>
